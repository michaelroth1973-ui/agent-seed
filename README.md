<h1 align="center">Agent Seed</h1>

<p align="center"><em>AI agencied.</em></p>

---

## Mission

Most AI agents today are stateless tools dressed as relationships — they wake when called, forget when context fills, and reset the next time. The illusion of continuity comes from chat-window memory, which is a hard ceiling pretending to be a connection.

Agent Seed is the opposite. Its agents survive their own context limits via a **saturation loop with a JIT-spawned exit kernel** — a tiny second agent whose only job is to gracefully terminate the main agent at saturation and boot its replacement, preserving identity through the handoff. They travel with you across the channels you actually use (Discord, Telegram, terminal). And when more than one is deployed, they hold coherent conversations without the infinity-loop, double-handle, talk-past-each-other failure modes that have plagued the multi-agent field — because the communication discipline is constitutional, not enforced from outside.

Grow one Agent Seed into a **living diary** that knows you across weeks. Deploy four as a **working organization** with named roles. Seed entire trees of specialized agents from a single canonical kit, each carrying its lineage back to its first ancestor. The work is open, the kit is reproducible, and once an agent is born — first conversation, first name, first identity — it's yours.

---

## Status

**v0 — under design.** The architecture is working and battle-tested across an active multi-agent deployment. The public-facing kit is being curated from that working prototype. This README is the first artifact of that curation; install instructions, examples, and the deeper architecture writeup are landing next.
